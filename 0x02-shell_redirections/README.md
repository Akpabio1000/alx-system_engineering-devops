# SHELL REDIRECTIONS

In this project, the following linux commands will be used:
echo, cat, head, tail, find, wc, sort, uniq, grep, tr, rev, cut and passwd.

It will also feature special characters, pipes and filters.

Solution of each tasks will be given.
